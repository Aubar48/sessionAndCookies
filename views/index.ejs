<!DOCTYPE html>
<html>

<head>
  <title>
    <%= title %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
  <link rel='stylesheet' href='/stylesheets/styles.css' />
</head>

<% if (session.usuario===undefined) { %>

  <body>
    <div class="container rounded-3 bg-secondary mt-2">
        <form class="row g-3" action="/" method="post">
          <div class="card col-md-4 border border-warning pb-3 ">
            <label for="name" class="form-label">Name</label>
            <input type="text" class="form-control border border-danger" id="name" name="name">
            <small class="text-danger ms-2">
              <%= locals.errors && (errors.name && errors.name.msg) %>
            </small>
          </div>
          <div class="card col-md-4 border border-warning">
            <label for="email" class="form-label">Email</label>
            <input type="text" class="form-control border border-danger" id="email" name="email">
            <small class="text-danger ms-2">
              <%= locals.errors && (errors.email && errors.email.msg) %>
            </small>
          </div>
          <div class="card col-md-4 border border-warning">
            <label for="age" class="form-label">Age</label>
            <input type="text" class="form-control border border-danger" id="age" name="age">
            <small class="text-danger ms-2">
              <%= locals.errors && (errors.age && errors.age.msg) %>
            </small>
          </div>
          <div class="card col-md-4 mb-2 border border-warning">
            <label for="color" class="form-label ">Color</label>
            <select id="color" class="form-select border border-danger" name="color">
              <option selected hidden value="">Select a color.</option>
              <option value="blue">Blue</option>
              <option value="green">Green</option>
              <option value="red">Red</option>
              <option value="yellow">Yellow</option>
              <option value="white">White</option>
            </select>
            <small class="text-danger ms-2">
              <%= locals.errors && (errors.color && errors.color.msg) %>
            </small>
          </div>
          <div class="card col-2 border border-warning ms-2 pb-2 mb-2 ">
            <div class="form-check d-flex justify-content-around align-items-center">
              <input class="form-check-input p-3 border border-danger" type="checkbox" id="recordarColor"
                name="recordarColor">
              <label class="form-check-label" for="recordarColor">
                Remember a color.
              </label>
            </div>
            <button type="submit" class="btn btn-warning">Sign in</button>
          </div>
          <picture class="d-flex justify-content-center ">
            <img src="https://media.giphy.com/media/EddY9gk1YkWAnImfKm/giphy.gif" alt="">
          </picture>
        </form>
    </>
  </body>
  <% } else { %>

    <body class="<%= session.usuario.color %>">
      <h1>Hi! <%= session.usuario.name %>, Choise a color: <%= session.usuario.color %>, your email is:
            <%= session.usuario.email %> and your age is: <%= session.usuario.age %>
      </h1>
      <a href="/more/">View more</a>
    </body>
    <% } %>


</html>